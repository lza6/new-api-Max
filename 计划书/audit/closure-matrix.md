# 功能闭环状态矩阵（Phase A 只读审计）

> 任务：对 backlog 节点逐一核实**真实代码是否存在、是否可用**，仅以源码为准，禁止仅凭文档宣称。
> 仓库：`C:\Users\Administrator.DESKTOP-EGNE9ND\Desktop\公益服务网关\new-api`（生产 v1.2.35）
> 日期：2026-09-20 | 方式：rg/graft/Get-Content 静态核实（未运行任何测试、未改源码、未 git 操作）
> 状态含义：DONE=代码+前端+测试齐全；PARTIAL=部分实现/缺关键能力；MISSING=不存在或仅文档提案；BROKEN=存在但不可用/报错

| 节点 | 状态 | 证据（文件:行） | 置信度 | 备注 |
|---|---|---|---|---|
| B2-2 usage-logs 详情「请求时间线」+ JSON 导出 | DONE | 前端：`web/src/features/usage-logs/lib/request-timeline.ts`（buildRequestTimeline/exportTimelineJson）、`components/dialogs/request-timeline-card.tsx`、`components/dialogs/details-dialog.tsx:547-575`（构建时间线）、`:734-737`（Export JSON 按钮）、`:1259-1264`（RequestTimelineCard）、`lib/log-export.ts`（buildLogExportPayload）；测试：`lib/__tests__/request-timeline.test.ts`、`lib/__tests__/log-export.test.ts`、`components/dialogs/__tests__/request-timeline-card.test.tsx`。后端详情配套：`controller/log.go:82 GetLogByKey`（/api/log/token）、`router/api-router.go:321`（GET /api/log/usage/:id/cost-detail）、`:372`（任务事件流） | 高 | 时间线为**前端由既有消费日志字段推导**（other.frt 首包、use_time、admin_info.use_channel 等，`request-timeline.ts` 头部注释显式声明零额外计算），后端无独立按 id 拉单条日志详情的接口（列表接口即全字段）——功能可用但架构为「推导式」，老日志缺字段优雅降级 |
| B2-3 onboarding 3 步引导 + 错误人话映射 | DONE | onboarding：`web/src/features/onboarding/components/onboarding-guide.tsx:65-89`（3 步：/keys、/pricing、/playground）、`:92-110`（跳过/关闭）+ `lib/storage.ts:25-41`（localStorage 按 user_id）、挂载 `web/src/components/layout/components/authenticated-layout.tsx:24,55`；测试 `features/onboarding/__tests__/onboarding-guide.test.tsx:97-153`（跳过持久化/多账号隔离）。错误人话：`web/src/lib/server-error-message.ts:159-164`（cooldown、ip_banned、model_not_found 三类）+ `getFriendlyErrorMessage` 映射，测试 `lib/server-error-message.test.ts:75-104` | 高 | 对照 B2-3-方案.md 的 3 个评估项均落地：冷却/IP封禁/模型不可用 + 注册登录后引导可跳过 + 组件/单测齐全 |
| B2-4 渠道冷却 hover + 任务 fail_reason hover | DONE | 前端冷却：`web/src/features/channels/components/channel-health-cell.tsx:196-207`（Cooldowns 计数、Cooling down + `formatRelativeTime(coolUntil)` 到期时间、`Reason:` + coolClassLabelKey）；后端 `service/channel_health_score.go:127-151`（CoolUntil + LastCoolClass）、`service/channel_cooldown.go:20-46`（含 429 Retry-After）；测试 `channel-health-cell.test.tsx:55-95`、`lib/__tests__/channel-health.test.ts`。fail_reason：`web/src/features/usage-logs/components/dialogs/task-details-dialog.tsx:251-262`（人话 getFriendlyErrorMessage + 原始 fail_reason 同屏）；i18n `web/src/i18n/locales/zh.json:1298-1299,2157-2158,2621,4364,6456` | 高 | 冷却 hover 完整（原因+到期时间+组件测试+i18n）；fail_reason 展示在**任务详情弹窗**内而非日志行内 hover——语义上满足「hover 人话+原始原因」，落点与方案略有差异 |
| B3-1 用户画像层 + profile-insights 页 | DONE | 后端：`service/user_profile/profile.go`（Weibull 远期衰减:122、进程内 sync.Map + Redis 双层缓存:131-184、GetUserProfile:481-503，TTL 60s）、`controller/user_profile.go:21-32`、路由 `router/api-router.go:99`（GET /api/user/profile/insights）；测试 `service/user_profile/profile_test.go`。前端：`web/src/routes/_authenticated/profile/insights.tsx`、`web/src/features/profile-insights/{api,index,types}.tsx`（api.ts 调 /api/user/profile/insights）、入口卡片 `web/src/features/profile/index.tsx:74-84`（Usage Insights → /profile/insights） | 高 | 缓存=进程内 sync.Map + Redis（RedisEnabled 时），非纯 Redis；三库兼容无方言 SQL |
| B4-1 任务事件流 SSE | DONE | 后端：`model/task_event.go:38`（TaskEvent 表，ID=seq 全局单调）、`service/task_event.go:49 RecordTaskEvent`（best-effort，失败仅 SysError）、`controller/task_event.go:57 TaskEventsSSE`（since query + Last-Event-ID:140-147、heartbeat、done 收流）、路由 `router/api-router.go:372`（GET /api/task/:task_id/events, UserAuth）；测试 `service/task_event_test.go:67`（since 续传无 gap）、`controller/task_event_test.go:83`。前端：`web/src/features/usage-logs/lib/task-event-stream.ts`（parseSseChunk/since）、`components/task-event-stream.tsx:44-132`（fetch+ReadableStream 带 Bearer、断线按 since=lastSeq 重连）、`__tests__/task-event-stream.test.tsx` | 高 | 事件写入点已接 submit/queued/claimed/progress/step_ratio/succeeded/failed/refunded/unconfirmed（service/task_polling.go:621、task_billing.go:261 等）；SSE 鉴权走 header（EventSource 不兼容，已用 fetch 流替代） |
| B5-2 微美元影子价 + 费用明细分段 | DONE | 影子价：`model/shadow_price.go:66 ComputeApiEquivalentUsd`、`model/log.go:353`（api_equivalent_usd 落库）；分段接口：`controller/cost_detail.go:53 GetLogCostDetail` + 路由 `router/api-router.go:321`（GET /api/log/usage/:id/cost-detail），测试 `controller/cost_detail_test.go`、`model/shadow_price_test.go`。前端费用卡：`web/src/features/usage-logs/components/dialogs/details-dialog.tsx:167`（BillingBreakdown）、`:243-247`（shadowUsdMicros→USD）、`:458-481`（ExplainBreakdown「Fee Explanation」）、测试 `__tests__/explain-breakdown.test.tsx` | 高 | 影子价仅展示不改计费数值（cost_detail.go:36 注释显式声明）；费用说明卡为「解释性」段落，非另一独立接口 |
| B5-3 通用 webhook（webhook_endpoints/HMAC/Ed25519/幂等/重试） | MISSING | 全仓无 `webhook_endpoints` 表/模型、无幂等键、无投递状态机、无重试退避、无 Ed25519（rg 全仓核实）。现有：`service/webhook.go:17-45`（仅 HMAC-SHA256 通知发送器 + X-Webhook-Signature/Bearer，供通知类使用）、支付 webhook 控制器 `controller/topup_stripe.go、topup_creem.go、topup_waffo.go、subscription_payment_*.go`、路由 `router/api-router.go:62-67`（Stripe/Creem/Waffo/Waffo-Pancake）；「webhook_endpoints 表 + 签名 + 幂等 + 重试」仅存在于提案 `计划书/下一步改进指南.md:360` | 高 | 属「方案已写、代码未落」：通用端点注册/幂等/重试子系统未实现；支付侧 webhook 未迁移。列为 MISSING（最可疑项之一） |
| B5-4 模型目录动态同步 | PARTIAL | 手动同步存在：`controller/model_sync.go:317 SyncUpstreamPreview`、`:413 SyncUpstreamModels`（上游 llm-metadata，ETag/body 缓存:30-45）、路由 `router/api-router.go:393-394`（GET /api/models/sync_upstream/preview、POST /api/models/sync_upstream）；幂等：`model/model_metadata_sync.go:38 ErrMetadataSyncConflict`、`:70 MetadataRecordVersion`、`:154 ApplyMetadataSync`（record-version 冲突检测 + 行锁 + 空表锚点:33-60）；审计：`middleware/audit.go:85`（model.sync_upstream）；测试 `controller/model_management_test.go:620-687`。**缺失**：无定时拉取调度（全仓无 ticker/cron 调用 SyncUpstreamModels）、无 `/v1/pricing` 路由（仅 `router/api-router.go:36` /api/pricing 前端定价页） | 高 | 已实现「预览→应用」手动同步 + 幂等 + 审计；「定时拉取」与「/v1/pricing 开放端点」未实现 → PARTIAL，缺 2 项关键能力 |
| B5-5 工具侧接入预设页 | DONE | `web/src/routes/tool-setup.tsx:46-119` buildPresets（Claude Code/`export ANTHROPIC_BASE_URL`、Cursor `.cursor/settings.json`、OpenCode、Codex CLI、Cline，均含 base_url+key+model）；i18n `web/src/i18n/locales/zh.json:6518-6527`（Claude Code/Cursor/OpenCode/Codex CLI/Cline）；导航 `web/src/hooks/use-top-nav-links.ts:109-113`（Tool Integration，受 HeaderNavModules.toolSetup 控制） | 高 | 5 个工具预设齐全，路由/导航/i18n 闭环；模型名 SITE_MODEL 为硬编码示例（deepseek-v4-flash） |
| S1 登录会话数量上限 | DONE | 开关：`common/constants.go:56-60`（SessionLimitEnabled 默认 false）、`model/option.go:51,339-340`（Option 持久化）；执行：`service/auth_session.go:75-81`（超限拦截）、`:450-451`（返回 AUTH_SESSION_LIMIT）、`model/user_session.go:34`（ErrUserSessionLimit）；接口 `router/api-router.go:93-95`（GET /api/user/sessions、DELETE /sessions/:sid、POST /sessions/revoke-others）；前端 `web/src/features/system-settings/security/login-session-limit-section.tsx:59-78`（admin 开关写入 SessionLimitEnabled）、`web/src/features/security/components/login-sessions-card.tsx:60-108` + `login-session-dialogs.tsx:69`（多设备管理/退出其他会话）；错误人话 `web/src/lib/server-error-message.ts` AUTH_SESSION_LIMIT；测试 `service/auth_session_test.go`、`controller/auth_session_test.go` | 高 | 默认关 ✓、admin 可开关 ✓、多设备会话列表+踢出 ✓；满足 spec Q1 假设（每用户会话上限策略可配置） |
| S2 模型测试 tab 入口 | DONE | 页面 `web/src/routes/model-test.tsx`（公开的模型效果测试页）；按钮/导航 `web/src/components/layout/config/top-nav.config.ts:33`（/model-test）、`web/src/hooks/use-top-nav-links.ts:104-106`（Model Effect Test，受 HeaderNavModules.modelTest 控制） | 高 | 顶导航可达（modelTest 默认开启）；「难访问」的旧反馈应已解决——入口是公开导航项而非设置内 |
| S3 日志/请求列表一键封禁 IP UI | DONE | 后端：`model/banned_ip.go`（banned_ips 表，BanIP/IsIPBanned，ExpiresAt=0 永久）、`router/web-protection-router.go:10-25`（GET/POST banned-ips、unban、unban-batch、DELETE :id）、`controller/web_protection.go`；前端：`web/src/features/web-protection/web-protection-page.tsx:194-227`（请求统计列表每行「Ban IP」按钮 + 封禁时长）、`:244-280`（已封禁 tab 解封/批量）、侧边栏 `web/src/hooks/use-sidebar-data.ts:174`（Web Protection，SUPER_ADMIN） | 高 | 说明：封禁入口在**管理侧 Web Protection 页的按 IP 请求统计列表**（rate/count/bytes + Ban IP），**不在 usage-logs 日志行内**——功能满足「请求列表一键封禁」但落点与用户原始诉求（日志页内）有出入，建议后续补日志行快捷入口 |
| S4 签到奖励额度设置报错「Invalid input」 | DONE（前端修复+回归测试锁住） | 前端校验：`web/src/features/system-settings/general/checkin-settings-section.tsx:60`（zod: enabled + minQuota/maxQuota >=0 支持小数）、提交链路 parseQuotaFromDollars→整数 quota→Option 键 `checkin_setting.min_quota/max_quota`；后端：`setting/operation_setting/checkin_setting.go:19-36`（Register checkin_setting）、`setting/config/config.go:165-226`（int 字段容错 float 字符串如 "2.000000"）；回归测试 `__tests__/checkin-settings-section.test.tsx:98-138`（明确断言「accepts decimal currency amounts without Invalid input」） | 中 | 全仓 Go 代码无 "Invalid input" 字符串（旧报错应来自旧 Option 键/整数字段 + 前端超整数额路径）。现状：表单小数→整数换算 + zod 下限 + 后端容错 + 回归测试已锁；**线上未实测**，属「有测试证据的修复」而非浏览器实证 |
| S5 管理端每秒并发 + 近1分钟完成 | DONE | 后端：`middleware/stats.go:11-35`（61 格按秒滑动窗口 completedWindow）、`:48-80`（activeConnections + CompletedLastMinute）、`controller/log.go:129-130`（GetLogsStat admin 返回 concurrent_requests/completed_last_minute）、`middleware/stats_test.go:16-89`；前端：`web/src/features/usage-logs/components/common-logs-stats.tsx:102,130-136`（admin 渲染 + refetchInterval 5s）、`types.ts:287-289`、`__tests__/common-logs-stats.test.tsx:63-64,113-115`；i18n 7 语言 `zh.json:6485-6486` 等；线上证据 `计划书/e2e-evidence/concurrency-live-metrics-e2e.json`、`prod-v1.2.35-live-metrics.json`（audit-ledger 记载 7/7 PASS，v1.2.35） | 高 | 与 日志页并发与完成指标方案.md 声明一致；audit-ledger 有本地 5 并发 + 线上验收记录 |
| S6 429 自动退避/RELAY_TIMEOUT/流量统计 | DONE | 429 退避：`controller/relay.go:255-259`（ShouldBackoff429 + Relay429MaxRetries/Relay429RetryDelayMs）、`service/channel_cooldown.go:63`（仅 429 Retry-After 生效）、`service/relay_error_class.go`（错误分类）；RELAY_TIMEOUT：`common/constants.go:169` + `common/init.go:112`（env RELAY_TIMEOUT）；流量统计：`controller/log.go:166 GetLogsTraffic` + 路由 `router/api-router.go:318`（/api/log/traffic）+ 前端 `usage-logs/api.ts:49-54` | 高 | audit-ledger：v1.2.30-34 已闭环，本次仅确认存在；429 重试/退避/超时/流量统计四件套齐全 |

## 统计
- 节点总数：15 | DONE：13 | PARTIAL：1（B5-4） | MISSING：1（B5-3） | BROKEN：0

## 前 5 最可疑/缺失项（按影响排序）
1. **B5-3 通用 webhook 子系统 MISSING**：全仓无 webhook_endpoints 表/幂等/重试/Ed25519，仅 `service/webhook.go` HMAC 通知发送器 + 支付 webhook（`router/api-router.go:62-67`）；`计划书/下一步改进指南.md:360` 仍是提案。
2. **B5-4 模型目录动态同步 PARTIAL**：手动 preview/apply 已闭环（`controller/model_sync.go:317,413` + `model/model_metadata_sync.go:154` + 审计 `middleware/audit.go:85`），但「定时拉取」与「/v1/pricing」缺失（全仓无调度器调用 SyncUpstreamModels，relay 侧无 pricing 路由）。
3. **B2-2 无按 id 单条详情后端接口**：列表接口即全字段 + cost-detail/events 补足，时间线由前端从既有字段推导（`request-timeline.ts` 头部声明零额外计算），非独立存储的 trace。
4. **S4 签到「Invalid input」修复只有前端测试证据**：后端仍无该错误串，回归测试 `checkin-settings-section.test.tsx:98-138` 锁住小数金额路径；线上未实测。
5. **S3 一键封禁落点在 Web Protection 页而非 usage-logs 日志行**：统计列表逐行 Ban IP（`web-protection-page.tsx:194-227`）可用，但日志明细页内无快捷封禁入口。