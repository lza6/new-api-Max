# 01-现状基线 v1.2.38

> 基线提交：8cedd781a（main）。以下全部来自 git log / VERSION / 审计账本的已落地事实。

## 版本与能力（v1.2.32 → v1.2.38）
| 版本 | 落地内容 |
|---|---|
| v1.2.32 | in-memory 渠道缓存降首字延迟；landing 前端性能（vendor 拆分/preload/lazy-load hero/SW/SEO）；check-in 支持小数金额；Go 代码评审 P1 修复 |
| v1.2.33 | check-in 设置 Invalid input + max<min 防护；浏览器级 E2E 证据（checkin/landing/SW） |
| v1.2.34 | 异步 consume/error log flush + 连接池并发；按请求流量字节统计 + 日流量端点；管理台今日/7d/30d 流量徽标；有界 429 backoff（wait 1s, 环境可调） |
| v1.2.35 | 日志页实时并发 + 完成指标（API + Web 徽标）；线上验收 |
| v1.2.36 | 三库迁移/启动幂等验证记录；生产部署验收 |
| v1.2.37 | token 创建返回 key + 默认无限；quota 耗尽 401 语义澄清；首字延迟根因证据 |
| v1.2.38 | relay 上游超时 → 504 Gateway Timeout；RELAY_TIMEOUT 300→900 |

## 审计结论（Phase A 与 SQL 修复）
- 审计账本（旧版 audit-ledger.md，已随计划书删除但结论留存）：
  - A1 节点 15：13D / 1P / 1M
  - A2 契约错误：13 条（P1×4）
  - A3 drift×1 + 映射缺口×3
  - A4 无 P0
- SQL 修复 F2/F3/F4：logs 复合索引 + task_events 索引与保留 → SQLite+PG15+MySQL8 迁移×2 幂等、三索引落库 **PASS**。

## 技术栈
- 后端 Go 1.25.1：Gin + GORM v2；`relaykit/` 独立模块。
- 前端 React 19 + TS + Rsbuild2 + TanStack + Zustand + Base UI + Tailwind4；i18n en/zh/zh-TW/fr/ru/ja/vi。
- 数据库 SQLite/MySQL(≥5.7.8)/PostgreSQL(≥9.6) 三库并行；日志库另支持 ClickHouse；Redis + 内存缓存。
- auth：session/API token/PAT/JWT/WebAuthn/TOTP/OAuth/OIDC + service/authz（Casbin）。
- 扩展：pkg/jsplugin（Sobek）任务插件；electron 桌面壳。