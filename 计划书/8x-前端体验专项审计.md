# 8.x UI/UX 与前端体验专项 —— 审计与补齐报告（v1.2.30）

日期：2026-09-19
状态：审计完成 + 缺口补齐（JSON 导出）+ 质量门通过

## 1. 逐项现状核对（代码证据）

### 8.1 组件反馈规范 —— 基本已满足
- Button loading/disabled：`web/src/components/ui/button` 全项目复用（loading 态由各操作处控制）
- ConfirmDialog / EmptyState / AutoSkeleton：`web/src/components/confirm-dialog.tsx`、`empty-state.tsx`、`auto-skeleton.tsx` 均存在并被 feature 复用
- getFriendlyErrorMessage：`web/src/lib/server-error-message.ts`（B6-2 已实现），task 详情/渠道错误等使用
- 结论：无需新建组件；抽查操作路径已复用共享组件

### 8.2 新手路径 —— 已满足
- Onboarding 引导：`web/src/features/onboarding/onboarding-guide.tsx` + storage + 测试（「三步快速上手：创建 Token→模型广场→首次请求」，可跳过）
- Playground 错误场景：`playground/components/message/message-error.tsx` + `message-error-actions.tsx`（错误人话 + Retry 一键重试 + 编辑提示词）
- 结论：已实现，无需改动

### 8.3 日志透明化 —— 已满足 + 本轮补 JSON 导出
- 费用解释卡（facts+inferences）：details-dialog.tsx（B5-1/B2-1）已实现 + explain-breakdown 测试
- 请求时间线：request-timeline-card.tsx + request-timeline.ts（B2-2）已实现 + 测试
- hover-why：渠道冷却 ChannelHealthCell Popover（B3-1/B2-4）、任务 fail_reason 人话 tooltip（B2-4）、结构化进度条（B5-3）
- **缺口（本轮补）**：usage-logs 详情缺「导出 JSON」按钮 → details-dialog.tsx 新增 Export JSON（完整 log 结构化 + timeline 下载为 .json，复用 Button，i18n 7 语言）

### 8.4 无障碍与国际化 —— 已满足
- 焦点可见 / aria-label：共享组件自带；新增 Export JSON 按钮带 aria-label
- i18n：所有新增文案走 i18n（本轮 'Export JSON' 已 7 语言），`bun run i18n:sync` 无漂移
- 色弱 A-F 徽章：渠道健康徽章为 A-F 字母 + 颜色双通道（非纯颜色）

### 8.5 前端质量门 —— 全绿
- `bun run typecheck`（tsgo -b）通过
- `bun run build` 通过
- vitest：explain-breakdown 5/5 + request-timeline-card 2/2（本轮相关）全绿
- oxlint 零告警（修复 prefer-template）
- 复用组件：Export JSON 复用 `@/components/ui/button`；无新造组件

## 2. 本轮改动
- `web/src/features/usage-logs/components/dialogs/details-dialog.tsx`：新增 handleExportJson（完整 log JSON + timeline 下载）+ title 区「导出 JSON」按钮（aria-label）
- i18n 7 语言 'Export JSON' 键

## 3. 验收
- [x] JSON 导出按钮渲染（截图 b8x_export_json.png）
- [x] 点击导出下载 .json 文件（E2E 验证 download 事件）
- [x] typecheck/build/vitest/oxlint/i18n 全绿