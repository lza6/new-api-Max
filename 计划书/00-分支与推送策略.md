# 00-分支与推送策略

## 2026-09-20 分支收敛记录（真实执行）
- 合并前本地分支：`main`(v1.2.31=24bb1d536)、`codex/audit-go-review`(v1.2.38=6da41bd02)、`codex/docs-prd-landing-perf`(464af8585)。
- 关系：`codex/audit-go-review` 含 main 后 36 个提交（v1.2.32→38 全部功能/审计/性能工作），merge-base = main 自身，可 fast-forward；`codex/docs-prd-landing-perf` 的独有提交已包含在其中。
- 执行：`git merge --ff-only codex/audit-go-review` → main = 6da41bd02；删除本地两分支；提交旧计划书删除（8cedd781a）；`git push origin main`（24bb1d536..8cedd781a）；删除远端 `codex/*` 两分支。
- 结果：本地与远端 **只保留 main**（远端验证 `git ls-remote --heads origin` 仅 main = 8cedd781a）。

## 清理项
- 损坏 tag 引用 `v1.2.11`（object 缺失导致所有 git 命令告警）已删除；如需可 `git fetch origin tag v1.2.11 --no-tags` 重建。

## 推送约定（用户明确要求，已记入长期记忆）
- 后续一切推送以 **main** 为主，不再为功能开常驻分支；临时工作可用本地分支，收尾一律并入 main。
- 上游 `upstream/QuantumNous/new-api` 仅作参考，不作为推送目标。
- 严格：不强制推送、不做破坏性 reset/clean、不用 `git add .`。