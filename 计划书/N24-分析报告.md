# new-api 参考项目对标分析报告（N24 综合版）

> 基于 1015 个参考项目全量盘点 + 20 个主题侦察组深读（~280 个项目，全部带文件证据）。
> 完整侦察报告存于 `scout_reports/`（20 份），状态台账见 `参考的结果计划指南.md`。
> 证据分级：`已验证`=实际读码/运行；`静态确认`=读文件确认；`合理推断`=多证据推断。

---

## 1. 项目识别结果

### 1.1 主项目识别结论
**new-api = Go 1.25 + Gin/GORM 单体网关 + React 19 嵌入式 SPA 的 AI API 聚合网关**（已验证）
- 250+ 管理 API 端点、45 个渠道适配器、relaykit 独立协议模块（33k 行零宿主依赖）
- 三种计费模式（倍率/按次/表达式）、35 张表 AutoMigrate、SQLite/MySQL/PG 三库兼容
- JS 任务插件体系（Sobek）：10 个内置插件（kling/sora/veo 等 4816 行）已验证"提交-轮询-结算"闭环
- 259 个 Go 测试文件（~69k 行测试代码）、前端 118 个测试文件 962 用例、6 个 CI 工作流

### 1.2 参考项目扫描结果（全量 1015 个）
| 类型 | 数量 | 处置 |
|------|------|------|
| node | 330 | 主题组深读 ~90 个 |
| python | 201 | 主题组深读 ~80 个 |
| skill 型（SKILL.md） | 188 | N21 全量分类 + 抽样深读 |
| doc-or-mixed | 195 | 分类归档，要点项目纳入 |
| go | 48 | 12 个深读（同语言工程优先） |
| rust | 35 | 8 个深读 |
| java/flutter/no-readme | 18 | 快速分类 |
| **价值分布** | — | 完全无参考价值 ~35-42%；边缘可借鉴 ~25-30%；**真正高价值 ~20-25%**（合理推断，N22 扫尾验证无重大漏网，新增漏网 10 个已回捞） |

### 1.3 高价值参考项目 Top 20（综合四维评分）
| 项目 | 定位 | 为什么值得参考 |
|------|------|---------------|
| **casbin-gateway**（Go） | 本机 agent 网关 | **Authenticity 渠道验真探测**（Go 同栈零迁移） |
| **freellmapi** | 免费池网关 | 冷却探针早恢复+配额预测+in-flight lease |
| **aisix**（Rust） | 企业网关 | cooldown/retryable 解耦+无锁配置快照+错误分类学 |
| **agent-framework-go**（MS） | Agent 框架 Go 版 | iter.Seq2 流式契约+Middleware 链+continuation token |
| **dsh-agent-teams** | 多 agent 编排 | 机器可判定质量门禁（任务合同 schema） |
| **agent-teams-ai** | 团队编排 | TokenUsageSourceKind 五级精度+三路成本拆分 |
| **MoneyPrinterTurbo** | 视频流水线 | **UnconfirmedTaskError 三态提交**防重复扣费 |
| **html-video** | HTML→视频 | seq+sinceSeq 断线续传事件流（~200 行参考实现） |
| **EcomAgent** | 电商 agent runtime | PreToolUse 三段管线+幂等键+行为回归指纹 |
| **commerce-agents**（Anthropic 官方） | 电商 agent | staging+provenance+host approval 安全三件套 |
| **baoyu-skills** | 生图技能集 | 后端解析链+验证生图真发生+9 类错误分类 |
| **awesome-gpt-image-2** | 提示词 SaaS | 预留额度→生成→回调结算完整计费闭环 |
| **SkillClaw** | 技能进化 | relay 层即技能进化拦截点——**与 new-api 形态最同构** |
| **TencentDB-Agent-Memory** | 记忆服务 | MemoryProxy=LLM 网关+按请求注入画像（同构） |
| **agent-deck**（Go） | agent 成本 | 微美元 int64 账本+预算同事务检查 |
| **agent-inspect** | 证据层 | Facts/Inferences 分离+首因故障定位 |
| **sandbox-runtime**（Anthropic） | OS 沙箱 | Windows 无容器沙箱（专用账户+WFP+两跳启动） |
| **hyperresearch** | Deep Research | 独立性审计+claim/evidence schema+对抗审计 |
| **SkillSpector**（NVIDIA） | skill 扫描 | 71 漏洞模式/33 分析器，装前安全门禁 |
| **beads**（Go） | issue tracker | conformance 契约套件（三库验证矩阵机器化） |

判断依据与逐项评分：见对应 scout_reports/（N2-N22），全部带文件路径证据。

---

## 2. 主项目现状总结

### 2.1 当前架构特点
单体 Go 网关按技术分层（router/middleware/controller/service/model/relay），layer 内聚类良好；relaykit 独立模块验证了协议层解耦；前端 26 个 features 统一目录结构。

### 2.2 当前优点（已验证，N1）
1. **计费/配额体系属同类开源网关第一梯队**：五层溢出防护（请求边界→decimal→饱和钳制+审计→倍数白名单→无符号上限）、BillingSession 两步提交、billingexpr 393 行设计文档+版本化+1190 行测试
2. **JS 插件协议边界干净**：宿主拥有连接/轮询/计费/落库，插件只做数据变换；5s 超时+并发准入+HookError 消毒
3. **三数据库兼容系统性执行**：集中保留字列、lockForUpdate 方言分支、迁移 dialector 分支
4. **多 master 部署意识**：DB-lease 去重、Casbin 同步、节点上报
5. 前端规范可执行（web/AGENTS.md 221 行+组件检索入口表+测试纪律）

### 2.3 当前主要问题（已验证）
1. **JSON 规范大面积违反自家规范**：relay/channel 十余个适配器直接 encoding/json（AGENTS.md 明令禁止）
2. Midjourney 旧轨 1647 行与新 task-plugin 双轨并存
3. RelayInfo 1184 行 God Object；distributor 中间件 735 行承载业务
4. 双轨定价（legacy 243 行硬编码表 vs 表达式）
5. 45 个渠道适配器仅 4 家有测试；e2e/浏览器测试缺位；CI 无 lint
6. gin v1.9.1 / go-redis v8 依赖滞后

### 2.4 小白用户体验薄弱点（7 条，已验证）
无部署后 onboarding（setup 4 步后无引导流）· 默认 root/123456 · **费用对普通用户黑盒**（tier 命中原因/缓存折扣只进 admin_info）· 定价页 11 个筛选维度无估算器 · 文档内嵌极薄（断网帮助断裂）· 渠道调试体验分散 · 无成本告警默认值

### 2.5 关键结构性缺口（合理推断，多组证据交叉）
1. **渠道可信度验证缺失**：渠道测试仅连通性/余额；参考集 3 个独立项目（casbin-gateway/tokentest/Elysia）都在做"上游验真/usage 防虚报"——公益网关聚合转发渠道时这是用户信任根基
2. **流式请求无法 fallover**：流一旦开始无法切换渠道（free-router 首包缓冲是最小代价解法）
3. **交互/webhook 型扩展无骨架**：5 个支付 webhook 各自为政，无通用 webhook/事件注册机制——电商动作类扩展撞墙
4. **任务只有 Submit→Poll 两态**：缺"unconfirmed 已付费待确认"态，退款可能造成资损纠纷

---

## 3. 参考项目亮点提炼（按主题组，泛化共性）

### 3.1 网关层共性（N2/N3/N4/N16）
- **路由智能化三件套**：凸组合因子评分（权重和=1）+ Beta 后验 Thompson 采样 + cache 亲和粘滞路由
- **错误处理解耦**：cooldown 与 retryable 分离、决策单点化（所有 dispatch 路径共用）、超时归因保留 cause chain
- **免费池生命周期**：冷却探针早恢复（探到过半程才探）、配额预测（"今天还能不能调"）、in-flight lease 防竞态
- **渠道验真**：11 维权重探测（模型身份/参数静默丢弃/缓存虚报/知识截止问答）A-F 评分
- **计费演进方向**：缓存双 TTL 桶、reasoning tokens 单列、微美元整数、tiered 分段计费 breakdown

### 3.2 Agent 生态对网关的需求（N5/N15/N17——"消费者视角"）
**P0（缺失即坏）**：双协议 SSE 原样透传、FC/工具调用透传（配对完整性）、thinking 签名往返、prompt cache 全链路（cache_control+双 token 拆分）、usage 分块累计
**高频痛点**：anthropic-beta 头透传白名单、上下文窗口语义化错误、可重试错误分类、per-request 成本、会话级预算熔断
**CUA 高价值客群特征**：单会话数千次调用、image tokens 与 text tokens 必须分列对账、按会话/任务预算硬熔断、**/pricing 稳定镜像可形成锁定**

### 3.3 "越用越懂用户"机制（N6/N7——用户的核心诉求）
1. **用户画像自动生成**（GSD user-profile：8 维度+置信度+证据，HIGH 直接照做 LOW 试探语气）——落地成本最低感知最强
2. **logs 表天然是 L0 事件流**：规则画像层先行（无 LLM 聚合：模型偏好/成本习惯/时段分布/错误率）→ 再加 LLM 异步抽取（TencentDB 批量判定 O(1) 化）
3. **技能进化三层**：SkillClaw 数据层（relay 拦截→摘要→发布门禁→共享，**relay 层即拦截点**）+ SkillOpt 安全层（有界 add/delete/replace 编辑+held-out 门禁+no-regression）+ EvoSkill 优化层
4. 记忆通用配方：RRF(k=60) 融合（四家共用）、类型化衰减（画像长半衰期/行为短半衰期）、失效不删除

### 3.4 黑匣子透明化（N8——"让小白理解产品"）
Top 设计：Facts/Inferences 分离解释卡（"未选渠道 B：其上一分钟 429"）、微美元账本+预算同事务检查、请求 Timeline 按请求 ID 配对、单文件甘特导出贴工单、Declared vs Observed 对照高亮（配置说走 A 实际走 B）、hover-why 状态看板、节省美元化（"本次帮你省了 $X"）、脱敏证据包可分享

### 3.5 任务系统升级（N10/N11——媒体/内容生成）
1. **三态提交**：confirmed-failed / unconfirmed-paid（远端可能已创建，带远端 task_id 兜底轮询）/ confirmed-success
2. **进度是事件不是百分比**：seq+sinceSeq 断线续传（html-video ~200 行）、结构化 ProgressEvent、partial/final 双级（流式）
3. **模板=参数空间显式化**：(输入契约 schema)+(参数空间)+(管线脚本)+(质检断言) 打包
4. **质量闭环=可执行断言**：check.mjs 几何预检、anti-slop 清单——**"质检失败=不计费"技术上完全可行**
5. **计费内建管线**：预留额度→生成→回调结算（gpt-image-2）、prompt 文件先行（审计凭据）、验证生图真发生

### 3.6 垂直扩展路线（N19/N20/N12）
- **首发两个垂直**：Deep Research 报告工厂（纯 token 消耗与计费同构、档位即 SKU、claim/evidence schema 质量可机检）、数学建模/论文（竞赛学生付费意愿+多角色分模型吃网关路由）
- **电商 agent 安全四件套**：PreToolUse 规则引擎（模型无法绕过）→ 写操作 staging（对话里说同意无效）→ provenance（只写本会话返回过的 id）→ 幂等键
- **技能市场窗口期**：SKILL.md 格式已收敛（接入成本低），无人做"托管+沙箱验证+计费"闭环——skill 运行依赖的模型调用/key 管理正是 new-api 主场

### 3.7 工程实践（N4——Go 同语言可直接套用）
conformance 契约套件（三库验证机器化）、泛型并行管道（trivy）、无锁配置快照（atomic.Pointer+不可变快照+代际戳）、冷却决策单点化、fixture 从引擎生成（FixedClock）、测试基建包化

---

## 4. 差距分析

| 层 | 主项目现状 | 参考集最佳实践 | 差距等级 |
|----|-----------|---------------|---------|
| **功能层** | LLM relay+异步视频任务+插件 | 渠道验真、流式 fallover、语义缓存、技能市场、用户画像 | **大**（3 项高价值功能完全缺失） |
| **架构层** | 单体分层清晰，relaykit 解耦 | cooldown 决策单点、通用 webhook/事件子系统、任务事件流 | 中（扩展交互型能力撞墙） |
| **工程层** | 单测/集成成熟 | conformance 三库矩阵、CI lint、e2e、测试基建包化 | 中 |
| **性能层** | 每请求写 DB 日志 | Redis 队列批量落库（litellm）、无锁配置快照、EWMA 选路 | 中 |
| **安全层** | OWASP ASVS 遵守、限流分层 | 渠道验真、SSRF 二次解析、密钥脱敏、滑动窗、canary 回归 | **大**（上游可信度维度缺失） |
| **运维层** | 6 CI 工作流、Docker 多阶段 | trivy 镜像扫描、宿主自检、价格快照版本化 | 小 |
| **UX 层** | 管理功能全但术语重 | onboarding 向导、错误→人话、时间轴日志、费用透明、hover-why | **大**（用户核心痛点） |
| **测试/真实体验** | 单元层扎实 | e2e/浏览器路径/真实用户路径验证 | 大 |

---

## 5. 可迁移 / 半可迁移 / 不建议迁移

### 5.1 直接可迁移（思想+代码形态都适配，~2 周内可见效）
| 项 | 来源 | 收益 | 成本 | 风险 |
|----|------|------|------|------|
| 流式首包缓冲 fallover | free-router server.mjs | 流式请求渠道故障无感切换（用户强感知） | ~100-200 行 Go | 低（需超时上限保护） |
| 渠道验真探测 | casbin-gateway（Go 同栈） | 渠道健康分 A-F，抓"转卖便宜模型" | 1-2 周 | 低（探测流量走自身渠道） |
| 状态同义词表 | Open-Generative-AI | 各 adaptor ParseTaskResult 收敛 | 数十行 | 零 |
| unconfirmed 提交态 | MoneyPrinterTurbo | 减少"已付费但被退款"资损 | 1-2 天 | 低 |
| 退款可见性 | Open-Generative-AI | "Refunded N credits"拼进 fail_reason | 半天 | 零 |
| 微美元整数/成本明细 | agent-deck | 计费精度对齐头部 | 中 | 需回归验证 |
| 密钥日志脱敏 helper | Sparrowgate/SafeLine | 错误消息不泄 key | 半天 | 零 |
| SSRF 二次解析 | ai-scanner/guardian | 渠道 base_url 防 rebinding | 1-2 天 | 低 |

### 5.2 半可迁移（思想可迁，需适配当前技术栈/分阶段）
| 项 | 来源 | 适配思路 |
|----|------|---------|
| 用户画像层 | TencentDB-Agent-Memory/GSD | 先"规则画像层"（无 LLM 聚合 logs 表），OpenLore 式证据字段+mnemosyne 式衰减；LLM 抽取异步批处理后续加 |
| 任务事件流 seq+sinceSeq | html-video | Task 旁表加单调序号+SSE Last-Event-ID 语义，~200 行参考实现 |
| 凸组合因子路由 | OmniRoute/litellm | 16 因子裁剪到 6-8 个（health/latency/cost/cacheAffinity/quality…），替换简单权重 |
| 三库 conformance 套件 | beads | 把 AGENTS.md 的三库人肉验证写成 Go 契约测试 |
| Agent 协议需求 P0 五项 | N5 四家框架 | 逐项核对现有 relay（thinking 签名往返/cache 拆分计量需重点验证） |
| JS 插件安全模型 | VulnClaw/autopentest-go | ExecutionGate 内容寻址审批+分层权限规则（Go 直接对照） |
| 长任务隔离升级 | sandbox-runtime/OpenSandbox | jsplugin 维持请求级 hook；长任务先进程级（srt 模式）→容器级 |
| 技能市场 | N21/N6 | SKILL.md 最小规范+SkillSpector 式扫描+网关计费托管差异化 |
| 小白 UX 三件套 | N18 | onboarding 动态向导、错误码→人话映射、时间轴日志 |
| Deep Research/建模垂直模板 | hyperresearch/MathModelAgent | 打包 skills+schema+验证脚本为任务模板，档位即 SKU |

### 5.3 不建议迁移（先进但不适配当前阶段）
- litellm 的 Prisma+Postgres 数据层与 rust 子项目（架构绑定其 SDK）
- 9router 的 MITM 拦截（本地证书+改写 IDE 指纹，合规风险）
- nexus-llm-router 的 60+ 策略枚举（18k 行单文件反面教材——策略必须收敛）
- aisix etcd 配置面（已有分布式 Redis 方案，改配置存储得不偿失）
- QuantMind（AGPL v3 不可入）；OpenNutriTracker（GPLv3 隔离）
- 微 VM 沙箱自建（daytona 归档证明纯开源托管难以为继——应以网关协同定位切入）
- WebRTC 信令代理（实时语音 GA 模式，等语音渠道需求明确后再做）
- 医疗/法律/交易下单垂直（牌照/PHI/中文数据面缺失）

---

## 6. 优先级优化路线图

### P0（安全/稳定性/核心体验，建议首批批准）
| # | 做什么 | 为什么 | 涉及模块 | 收益 | 风险 | 数据迁移 | 兼容性 | 验证 | 回滚 |
|---|--------|--------|---------|------|------|---------|--------|------|------|
| P0-1 | **渠道验真探测**（题库+协议包络+缓存虚报检测，A-F 健康分） | 公益网关聚合转发渠道，"挂羊头卖狗肉"直接摧毁信任；3 个参考项目独立做同一件事 | controller/channel-test、新 service/channel_probe、前端渠道健康列 | 渠道可信度可量化 | 探测消耗少量上游 token（可配额） | 否 | 纯增量 | 真机三库+对已知良/劣渠道各验 | 删探测任务即可 |
| P0-2 | **密钥安全加固**（日志/错误路径密钥 HMAC 指纹化+SSRF 二次解析+删除 full key 回显） | 参考集 P0 共识；渠道 key 泄露是网关最大安全事故源 | middleware、relay、controller/channel | 安全合规 | 低 | 否 | 无破坏 | 安全回归测试 | git revert |
| P0-3 | **流式首包缓冲 fallover** | 流式请求无法切换渠道是核心可靠性缺口 | relay/helper、relay_adaptor | 流式可用性大增 | 中（首字节延迟+超时保护） | 否 | 无破坏（缓冲上限内） | 流式集成测试+真实渠道演练 | 配置开关关闭 |
| P0-4 | **cooldown/retryable 解耦+决策单点化** | 现有 auto-disable 混用 401/429 语义；新 relay 格式易漏冷却 | relay/channel、service/channel-health（新增） | 渠道治理精细化 | 中 | 否 | 需迁移现有禁用逻辑 | 单测+真机 | 保留旧逻辑开关 |
| P0-5 | **任务三态提交（unconfirmed-paid）** | 提交超时盲退款造成资损/纠纷 | service/task_polling、adaptor 契约 | 资损减少 | 低 | Task.Data 加字段（json 兼容） | 向后兼容 | 模拟超时+兜底轮询 | 状态回退兼容 |

### P1（高价值改造）
| # | 做什么 | 为什么 | 涉及模块 |
|---|--------|--------|---------|
| P1-1 | 用户画像层（规则版）：logs 聚合→模型偏好/成本习惯/时段/错误率+画像页 | "越用越懂用户"最小可行版本，零 LLM 成本 | model、service、web 新 feature |
| P1-2 | 任务事件流+结构化进度+状态同义词表 | 轮询风暴/进度丢失/各 adaptor 词汇不一 | model/task、router、各 adaptor |
| P1-3 | 日志透明化四件套：解释卡（Facts/Inference）、hover-why、甘特导出、退款可见性 | 用户"为什么扣这么多/为什么选这个渠道" | service/log_info_generate、web usage-logs |
| P1-4 | onboarding 向导+错误码→人话映射+成本告警默认开启 | 小白 7 薄弱点前 3 | web/setup、controller、web 组件 |
| P1-5 | Agent 协议 P0 五项核对（thinking 签名/cache 拆分/beta 头白名单/usage 分块/FC 配对） | agent 生态是增长客群，缺失即坏 | relaykit、relay |
| P1-6 | 计费现代化：微美元整数+cache_read/creation 双桶+tiered breakdown | 计费保真是新一代竞争点（Elysia 已做） | common/quota_math、types.PriceData |
| P1-7 | 三库 conformance 契约套件+CI lint 门禁 | 把人肉三库验证机器化；lint 规则落地 | model、.github/workflows |
| P1-8 | Redis 队列批量落库+无锁配置快照 | 高 QPS 日志写压+配置热更新 | model/log、common/config |

### P2（锦上添花/战略方向）
- 用户 skill 沉淀与市场（SkillClaw 拦截管线+SkillSpector 扫描+托管计费差异化）
- Deep Research / 数学建模垂直任务模板（打包 skills+schema+验证脚本）
- 电商 agent 能力（需先建通用 webhook/事件子系统——EcomAgent 四件套为蓝本）
- 文档知识库增值（LightRAG PG 一体化 / PageIndex 树推理）
- 实时语音 WebRTC 传输矩阵、沙箱长任务隔离升级、多 agent 编排增值（ccteam 账本+policy hook）

---

## 7. 全栈实施方案（按 P0 批次）

**后端**：新增 `service/channel_probe.go`（验真任务，复用 SystemTask DB-lease 模式）；`relay/helper` 首包缓冲；`service/channel_health` cooldown/retryable 状态机；task adaptor 契约加 `UnconfirmedTaskError` 语义
**前端**：渠道健康列 A-F 徽章；usage-logs 解释卡；setup 完成后 onboarding flow（复用 section-registry 模式）
**数据层**：渠道表加 probe 结果字段（json）；Task.Data 约定 unconfirmed 状态与 lineage；无破坏性迁移（全部 AutoMigrate 增列）
**接口层**：`/v1/pricing` 稳定镜像端点（P2 提前小步做）；任务查询加 Last-Event-ID 语义（P1）
**配置管理**：探测频率/题库开关/预算参数走 config.GlobalConfig
**日志监控**：密钥 HMAC 指纹化中间件；probe 结果入渠道事件流
**测试策略**：验真题库单测（对已知良/劣渠道）→ 流式 fallover 集成测试 → 三库 conformance 套件（P1-7 一起落）
**部署运维**：全部纯增量，无迁移；逐批配置开关可回滚

**建议批次顺序**：P0-2（安全，最低风险先行）→ P0-5（三态提交）→ P0-3（流式 fallover）→ P0-4（cooldown 解耦）→ P0-1（渠道验真，最重最后）

---

## 8. 实施授权状态

**当前未授权修改源代码。** 建议优先批准批次：

| 批次 | 内容 | 影响范围 | 风险 | 预计验证 |
|------|------|---------|------|---------|
| Batch-1 | P0-2 + P0-5（密钥脱敏+三态提交） | middleware/relay/service/task，~10 文件 | L1-L2 | 单测+定向集成 |
| Batch-2 | P0-3 + P0-4（流式 fallover+cooldown 解耦） | relay/helper+channel 治理，~8 文件 | L2（行为变化，需开关） | 单测+真机流式演练 |
| Batch-3 | P0-1 渠道验真 | 新增 service+controller+前端，~12 文件 | L2（纯增量） | 真机三库+优劣渠道对照 |

**需单独审批事项**：任何数据库迁移演练（三库矩阵）、探测功能消耗上游 token 的预算上限、ci.yml 修改（P1-7）。

---

## 9. 需要我补充的信息

1. **渠道验真的探测预算**：探测流量消耗真实上游 token（每渠道每轮 ~10 次调用）。默认建议：每渠道每日 1 轮+仅管理员手动触发可增强——是否可接受？（不提供则默认保守档）
2. **流式 fallover 的首包超时**：参考实现用"缓冲直到首个 content/tool_call"，超时建议默认 15s（可配）。是否同意默认值？
3. **P1-1 用户画像层的展示位置**：用户侧个人页 vs 管理侧用户详情？（建议两者，用户侧只展示用户自己可解释的摘要）
4. **语言**：新增 UI 文案按项目惯例走 i18next 英文键（zh 为 fallback）——确认无异议。
5. **探测结果可见性**（可选）：不提供则按"管理员可见详情、用户只见健康档位"实施。

---

*报告完毕。全量证据链：`scout_reports/`（20 份）+ `参考的结果计划指南.md`（状态台账）。等待批准实施范围后进入 Phase B（实施前影响分析 → 分批实施 → 独立 Critic 审查 → 复验）。*
